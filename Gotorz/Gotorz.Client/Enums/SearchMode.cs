namespace Shared.Enums
{
    // Enum to control SearchMode for SearchResults.razor (it uses the same space for flight and hotel results - SearchMode controls which is shown)
    public enum SearchMode
    {
        None,
        Flights,
        Hotels
    }
}
